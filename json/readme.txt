retorno em JSON, qualquer aplicação pode usar esses arquivos.

js_buscar = retorna a busca por POIs próximos. Necessita POST com 3 valores = X, Y e distância.

js_cadastrar = realiza a inserção de um POI no banco de dados.

js_listar = retorna JSON dos POIs cadastrados.